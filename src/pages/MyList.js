import AddPage from '../components/AddPage';

export default function MyList() {
  return (
    <main>
      <AddPage />
      <section>
        <h2 className="font-medium text-slate-600 tracking-wider">My List</h2>
      </section>
    </main>
  );
}
